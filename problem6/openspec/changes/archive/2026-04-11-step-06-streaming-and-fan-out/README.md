# step-06-streaming-and-fan-out

Stories 2.7-2.13: NATS JetStream client + StreamBootstrap, JetStreamEventPublisher, outbox publisher worker with leader election (GAP-04), coalescing window, ephemeral push consumer, SSE controller, GAP-02 SSE backpressure (DECISION)
