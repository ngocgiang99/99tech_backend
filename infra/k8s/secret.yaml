# =============================================================================
# WARNING: This is a STUB. Do NOT check in real secrets.
# Populate via External Secrets Operator, Sealed Secrets, or your cloud
# provider's secret manager (AWS Secrets Manager, GCP Secret Manager, etc.).
#
# The keys listed here are the exact env var names the application expects.
# Their base64-encoded values must be supplied by the operator's secret
# management pipeline — never hardcoded here.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: scoreboard-secret
  labels:
    app: scoreboard
  annotations:
    # TODO: if using External Secrets Operator, add the ESO annotation here
type: Opaque
data:
  # TODO: Populate these via your secret manager. DO NOT commit real values.
  # Values must be base64-encoded strings.
  INTERNAL_JWT_SECRET: ""  # TODO: base64(your-internal-jwt-secret, min 32 chars)
  ACTION_TOKEN_SECRET: ""  # TODO: base64(your-action-token-secret, min 32 chars)
  ACTION_TOKEN_SECRET_PREV: ""  # TODO: base64(previous-action-token-secret for rotation, min 32 chars)
