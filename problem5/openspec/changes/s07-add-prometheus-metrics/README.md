# s07-add-prometheus-metrics

Prometheus integration with prom-client: HTTP duration histogram, default Node metrics, custom cache/db/domain metrics, /metrics endpoint, optional compose prometheus service
